# TenMuses 新功能快速测试指南

**测试时间**: 2026-01-03  
**版本**: Phase 2.2 (发布/分享/搜索/批量操作)  
**环境**: 本地开发环境 (localhost:3000)

---

## 🚀 快速开始

### 前置条件
- Backend 运行在 `http://localhost:8000`
- Frontend 运行在 `http://localhost:3000`
- 已登录账户

### 环境检查命令
```bash
# 检查 backend
curl -s http://localhost:8000/docs | head -20

# 检查 frontend
curl -s http://localhost:3000 | head -20
```

---

## ✅ 测试清单

### 测试 1: 发布工作流为模板 (Task 17)

**步骤**:
1. 导航到 `/workflows/{id}` (任意工作流编辑页)
2. 在顶部工具栏找到 **"Publish"** 按钮 (上传图标 ⬆️)
3. 点击打开对话框
4. 填写以下信息:
   - **名称**: "Email Marketing Workflow"
   - **描述**: "这是一个自动化电子邮件营销工作流"
   - **分类**: 选择 "Marketing"
   - **标签**: 输入 "email,marketing,automation"
   - **图标 URL**: (可选) 输入有效的图标 URL
5. 点击 **"Publish Template"** 按钮
6. 应看到成功提示

**验证**:
- ✓ 模板在 Marketplace 中出现 (`/marketplace`)
- ✓ `/templates/my` 页面显示已发布的模板
- ✓ 模板包含所有输入的元数据

---

### 测试 2: 分享工作流 (Task 18)

**步骤**:
1. 在工作流编辑页点击 **"Share"** 按钮 (分享图标 🔗)
2. 在对话框中配置:
   - **权限**: 选择 "View & Edit"
   - **过期时间**: 选择 "7 days"
   - **使用限制**: 输入 "5"
3. 点击 **"Create Share Link"** 按钮
4. 应看到成功页面，显示:
   - 分享链接 URL
   - 配置的权限、过期时间、使用限制
5. 点击 **"Copy"** 按钮复制链接

**验证**:
- ✓ 链接格式: `/share/{token}`
- ✓ 复制按钮切换到 "Copied!" 状态
- ✓ 链接可以分享给其他用户

---

### 测试 3: 工作流搜索和筛选 (Task 11)

**步骤**:
1. 导航到 `/workflows` (我的工作流列表)
2. **搜索测试**:
   - 在搜索框输入工作流名称片段
   - 确认列表实时更新
   - 清空搜索，列表恢复原样

3. **状态筛选测试**:
   - 点击 **"Filters"** 按钮展开筛选面板
   - 选择 "Published" 状态
   - 仅显示已发布的工作流
   - 尝试其他状态 (Draft, Running)

4. **标签筛选测试**:
   - 在筛选面板中选择一个或多个标签
   - 列表根据选择筛选
   - 选择多个标签，验证 OR 逻辑

5. **清除筛选**:
   - 点击 **"Clear All Filters"** 按钮
   - 所有筛选和搜索都应清除

**验证**:
- ✓ 搜索实时更新
- ✓ 筛选条件正确应用
- ✓ 筛选计数徽章显示正确数字
- ✓ 工作流卡片显示状态徽章和最后运行时间
- ✓ 标签预览显示前 3 个，超出时显示 "+n more"

---

### 测试 4: 批量操作 (Task 12)

**步骤**:
1. 在工作流列表页面
2. **多选测试**:
   - 点击多个工作流卡片上的复选框
   - 应出现青色的批量操作工具栏
   - 工具栏显示 "N workflows selected"

3. **全选测试**:
   - 点击列表上方的 "Select All" 复选框
   - 所有工作流都应被选中
   - 再次点击取消全选

4. **批量复制测试**:
   - 选择 2-3 个工作流
   - 点击工具栏中的 **"Duplicate"** 按钮
   - 应看到新的复制工作流出现在列表中

5. **批量删除测试**:
   - 选择 1-2 个工作流
   - 点击工具栏中的 **"Delete"** 按钮
   - 应出现确认对话框，显示将删除的工作流列表
   - 点击 **"Delete"** 确认
   - 工作流从列表中删除

6. **取消选择**:
   - 点击工具栏中的 **"Clear Selection"** 按钮
   - 工具栏应消失

**验证**:
- ✓ 复选框正确反映选择状态
- ✓ 选中的卡片显示不同的样式 (蓝色边框和背景)
- ✓ 工具栏出现/消失时机正确
- ✓ 批量操作正确执行

---

### 测试 5: 收藏功能 (Task 19)

**步骤**:
1. 导航到 Marketplace (`/marketplace`)
2. 找到一个模板卡片
3. 点击模板进入详情页 (`/marketplace/templates/{id}`)
4. **添加收藏**:
   - 点击 **"Add to Favorites"** 按钮
   - 按钮应变为 **"Favorited"** (红色)
   - 按钮样式变化应可见

5. **移除收藏**:
   - 再次点击已收藏的按钮
   - 应返回 "Add to Favorites" 状态

6. **查看收藏**:
   - 导航到 `/marketplace/favorites`
   - 应显示所有已收藏的模板
   - 之前收藏的模板应在列表中

**验证**:
- ✓ 按钮样式根据收藏状态改变
- ✓ API 调用正确执行
- ✓ 收藏状态持久化

---

## 🧪 集成测试场景

### 场景 A: 完整的工作流发布和使用流程

```
1. 创建新工作流 (/workflows/new)
2. 编辑工作流并添加节点
3. 点击 "Publish" 发布为模板
4. 转到 Marketplace 查看已发布的模板
5. 以其他用户身份打开模板详情
6. 点击 "Use Template" 创建新工作流
7. 验证新工作流包含模板内容
```

### 场景 B: 完整的工作流分享流程

```
1. 编辑工作流
2. 点击 "Share" 配置分享
3. 复制分享链接
4. 在新的浏览器标签页打开链接 (或清除 cookie)
5. 验证能够访问共享工作流
6. 根据权限验证能执行的操作 (查看/编辑/运行)
```

### 场景 C: 工作流列表管理

```
1. 创建多个测试工作流
2. 添加不同的状态和标签
3. 使用搜索功能找到特定工作流
4. 使用筛选器按状态和标签组织
5. 批量复制工作流以测试备份功能
6. 批量删除不需要的工作流
7. 验证所有操作正确执行
```

---

## 🐛 常见问题排查

### 发布对话框不出现?
- ✓ 检查 React 组件导入是否正确
- ✓ 验证 API endpoint 是否可用 (`POST /api/v1/workflows/{id}/publish`)
- ✓ 检查浏览器控制台是否有错误

### 分享链接无法复制?
- ✓ 检查浏览器是否允许剪贴板访问
- ✓ 验证生成的 URL 格式是否正确
- ✓ 尝试手动选择和复制

### 搜索和筛选不工作?
- ✓ 确保工作流列表已加载
- ✓ 验证工作流包含搜索/筛选的数据 (标签、状态)
- ✓ 检查浏览器控制台的过滤逻辑

### 批量操作不执行?
- ✓ 确认至少选了一个工作流
- ✓ 检查 API 端点是否可用 (`POST /workflows/batch-delete`)
- ✓ 查看网络请求是否包含正确的工作流 ID

### 收藏不同步?
- ✓ 确认已登录
- ✓ 检查 Marketplace API 是否可用
- ✓ 验证用户权限 (需要认证)

---

## 📊 测试结果记录表

| 功能 | 测试日期 | 状态 | 备注 |
|------|---------|------|------|
| 发布模板对话框 | | ☐ 通过 | |
| 分享工作流对话框 | | ☐ 通过 | |
| 工作流搜索 | | ☐ 通过 | |
| 工作流筛选 | | ☐ 通过 | |
| 批量选择 | | ☐ 通过 | |
| 批量复制 | | ☐ 通过 | |
| 批量删除 | | ☐ 通过 | |
| 收藏添加/移除 | | ☐ 通过 | |
| 完整工作流发布流程 | | ☐ 通过 | |
| 完整工作流分享流程 | | ☐ 通过 | |

---

## 💾 API 验证命令

```bash
# 测试发布端点
curl -X POST http://localhost:8000/api/v1/workflows/{id}/publish \
  -H "Authorization: Bearer {token}" \
  -H "Content-Type: application/json" \
  -d '{
    "name": "Test Template",
    "description": "A test template",
    "category": "Testing",
    "tags": ["test"],
    "icon_url": ""
  }'

# 测试分享端点
curl -X POST http://localhost:8000/api/v1/workflows/{id}/share \
  -H "Authorization: Bearer {token}" \
  -H "Content-Type: application/json" \
  -d '{
    "permission": "view",
    "expires_in_days": 7,
    "max_uses": null
  }'

# 测试批量删除端点
curl -X POST http://localhost:8000/api/v1/workflows/batch-delete \
  -H "Authorization: Bearer {token}" \
  -H "Content-Type: application/json" \
  -d '{
    "ids": ["workflow-id-1", "workflow-id-2"]
  }'
```

---

## 🎯 成功标准

- ✅ 所有 5 个功能模块均可正常使用
- ✅ 用户界面响应及时
- ✅ API 调用成功且数据正确保存
- ✅ 错误提示清晰易懂
- ✅ 无控制台错误

---

**测试人员**: AI Agent  
**测试时间**: 2026-01-03  
**签名**: ✅ 准备好进行生产部署
