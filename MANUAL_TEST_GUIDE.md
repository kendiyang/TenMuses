# 🧪 TenMuses v2.4.0 - 人工测试指南

**启动时间**: 2026-01-03  
**测试类型**: 人工功能和集成测试  
**项目版本**: v2.4.0

---

## 📋 测试准备

### 服务状态

```
✅ 后端 API (FastAPI)
   - URL: http://localhost:8000
   - API 文档: http://localhost:8000/docs
   - 虚拟环境: /Users/mg/Workspace/TenMuses/backend/venv
   - 状态: 启动中...

✅ 前端应用 (Next.js)
   - URL: http://localhost:3000
   - 框架: Next.js 14.2.35 App Router
   - 状态: 启动中...

✅ 依赖服务 (Docker)
   - PostgreSQL: localhost:5432
   - Redis: localhost:6379
   - MinIO: localhost:9000
   - 状态: 运行中
```

---

## 🎯 人工测试计划

### 测试 1: 前端应用基本功能（5-10 分钟）

#### 步骤 1.1: 打开应用
- [ ] 打开浏览器访问 http://localhost:3000
- [ ] 验证应用加载成功
- [ ] 检查导航栏是否显示
- [ ] 检查页面是否响应式

**期望结果**: 
- 应用完整加载
- 所有元素正常显示
- 页面响应快速

#### 步骤 1.2: 检查页面布局
- [ ] 查看主页面布局
- [ ] 检查导航菜单
- [ ] 验证页面导航链接
- [ ] 检查移动端响应

**期望结果**:
- 布局清晰合理
- 菜单易于使用
- 链接可点击

#### 步骤 1.3: 检查加载性能
- [ ] 打开浏览器开发者工具 (F12)
- [ ] 检查 Network 标签
- [ ] 查看加载时间
- [ ] 检查资源加载情况

**期望结果**:
- 首屏加载 < 3 秒
- 所有资源加载成功
- 无红色错误

---

### 测试 2: 后端 API 文档和基本端点（5-10 分钟）

#### 步骤 2.1: 访问 API 文档
- [ ] 打开浏览器访问 http://localhost:8000/docs
- [ ] 验证 Swagger UI 加载成功
- [ ] 查看 API 列表
- [ ] 检查各个端点

**期望结果**:
- Swagger UI 完全加载
- 所有端点可见
- 文档格式正确

#### 步骤 2.2: 测试健康检查端点
- [ ] 在 Swagger UI 中找到 `/` 或 health check 端点
- [ ] 点击 "Try it out"
- [ ] 点击 "Execute" 按钮
- [ ] 查看响应

**期望结果**:
- 返回 200 状态码
- 响应格式正确
- 返回有效的 JSON

#### 步骤 2.3: 测试工作流 API
- [ ] 找到 `/api/v1/workflows` 端点
- [ ] 点击 "Try it out"
- [ ] 执行 GET 请求
- [ ] 查看返回的工作流列表

**期望结果**:
- 返回 200 或 201 状态码
- 返回有效的 JSON 数组或对象
- 数据结构合理

---

### 测试 3: 用户注册和认证（10-15 分钟）

#### 步骤 3.1: 前端注册
- [ ] 在前端应用中找到注册或登录页面
- [ ] 填写注册表单（邮箱、密码、用户名）
- [ ] 提交表单
- [ ] 查看是否成功或显示错误

**期望结果**:
- 表单验证成功
- 收到确认消息或重定向到登录
- 无崩溃或错误

**如果没有前端注册页面**:
- [ ] 使用 Swagger UI 的 `/api/v1/auth/register` 端点
- [ ] 输入必要参数（邮箱、密码、用户名）
- [ ] 执行请求
- [ ] 验证返回的 token

#### 步骤 3.2: API 认证测试
- [ ] 在 Swagger UI 中找到 `/api/v1/auth/register` 端点
- [ ] 点击 "Try it out"
- [ ] 输入测试用户数据：
  ```json
  {
    "email": "test@example.com",
    "password": "TestPassword123!",
    "username": "testuser"
  }
  ```
- [ ] 执行请求
- [ ] 记下返回的 token

**期望结果**:
- 返回 200/201 状态码
- 返回 access_token
- Token 格式为 JWT

#### 步骤 3.3: 使用 Token 授权
- [ ] 复制得到的 access_token
- [ ] 点击 Swagger UI 右上角的 "Authorize" 按钮
- [ ] 选择 Bearer Token 认证
- [ ] 粘贴 token：`Bearer <your_token>`
- [ ] 点击 Authorize

**期望结果**:
- 授权成功
- 后续请求会自动使用这个 token

---

### 测试 4: 工作流功能（15-20 分钟）

#### 步骤 4.1: 创建工作流
- [ ] 在 Swagger UI 中找到 `POST /api/v1/workflows` 端点
- [ ] 点击 "Try it out"
- [ ] 输入工作流数据：
  ```json
  {
    "name": "Test Workflow 1",
    "description": "Manual test workflow",
    "nodes": [],
    "edges": []
  }
  ```
- [ ] 执行请求

**期望结果**:
- 返回 201 Created
- 返回创建的工作流对象
- 工作流包含 ID、名称、描述
- 可以记下工作流 ID

#### 步骤 4.2: 获取工作流列表
- [ ] 找到 `GET /api/v1/workflows` 端点
- [ ] 点击 "Try it out"
- [ ] 执行请求
- [ ] 查看返回的工作流列表

**期望结果**:
- 返回 200 状态码
- 包含之前创建的工作流
- 列表格式正确

#### 步骤 4.3: 获取单个工作流
- [ ] 找到 `GET /api/v1/workflows/{workflow_id}` 端点
- [ ] 点击 "Try it out"
- [ ] 输入之前记下的工作流 ID
- [ ] 执行请求

**期望结果**:
- 返回 200 状态码
- 返回完整的工作流对象
- 数据与创建时一致

#### 步骤 4.4: 更新工作流
- [ ] 找到 `PUT /api/v1/workflows/{workflow_id}` 端点
- [ ] 点击 "Try it out"
- [ ] 输入工作流 ID
- [ ] 修改工作流数据（如名称）
- [ ] 执行请求

**期望结果**:
- 返回 200 状态码
- 返回更新后的工作流
- 修改已生效

---

### 测试 5: Marketplace 和模板功能（10-15 分钟）

#### 步骤 5.1: 获取模板列表
- [ ] 找到 `GET /api/v1/marketplace/templates` 端点
- [ ] 执行请求
- [ ] 查看模板列表

**期望结果**:
- 返回 200 状态码
- 返回模板列表
- 包含模板名称、描述、创建者信息

#### 步骤 5.2: 获取特色模板
- [ ] 找到 `GET /api/v1/marketplace/featured` 端点
- [ ] 执行请求
- [ ] 查看特色模板

**期望结果**:
- 返回 200 状态码
- 返回特色模板列表
- 可能为空或包含部分模板

#### 步骤 5.3: 获取模板详情
- [ ] 如果列表不为空，找到 `GET /api/v1/marketplace/templates/{template_id}` 端点
- [ ] 输入一个模板 ID
- [ ] 执行请求

**期望结果**:
- 返回 200 状态码
- 返回完整的模板详情
- 包含 nodes、edges 等工作流配置

---

### 测试 6: 缓存性能测试（10-15 分钟）

#### 步骤 6.1: 测试 Redis 缓存（首次查询）
- [ ] 在浏览器开发者工具中打开 Network 标签
- [ ] 清除过滤器，显示所有请求
- [ ] 访问 `http://localhost:8000/api/v1/marketplace/templates`
- [ ] 记录首次请求的时间 (例如: 45ms)

**记录结果**:
```
首次请求时间: ___ ms
```

#### 步骤 6.2: 测试缓存命中（第二次查询）
- [ ] 在 1-2 秒后再次访问相同的 URL
- [ ] 或在 Swagger UI 中再次执行相同的请求
- [ ] 记录第二次请求的时间 (例如: 1.5ms)

**记录结果**:
```
第二次请求时间: ___ ms
加速倍数: 首次 ÷ 第二次 = ___ x
```

**期望结果**:
- 第二次请求明显更快
- 加速倍数应该 > 10x
- 理想情况 20-50x

---

### 测试 7: 错误处理（5-10 分钟）

#### 步骤 7.1: 测试 404 错误
- [ ] 访问不存在的工作流：`http://localhost:8000/api/v1/workflows/nonexistent`
- [ ] 查看返回的错误

**期望结果**:
- 返回 404 Not Found
- 返回有意义的错误信息
- JSON 格式正确

#### 步骤 7.2: 测试 401 未授权
- [ ] 在未授权状态下访问受保护的端点
- [ ] 或清除 Swagger UI 中的授权信息
- [ ] 尝试访问需要认证的端点（如创建工作流）

**期望结果**:
- 返回 401 Unauthorized
- 返回清晰的错误消息

#### 步骤 7.3: 测试验证错误
- [ ] 找到一个需要特定参数的端点
- [ ] 故意提供无效数据
- [ ] 例如：创建工作流时提供无效的 JSON

**期望结果**:
- 返回 422 Validation Error 或 400 Bad Request
- 返回详细的验证错误信息

---

### 测试 8: 前后端集成（10-15 分钟）

#### 步骤 8.1: 前端调用 API
如果前端有功能可用：
- [ ] 在前端应用中执行某个操作（如创建工作流）
- [ ] 打开浏览器开发者工具的 Network 标签
- [ ] 观察对后端 API 的调用
- [ ] 查看请求和响应

**期望结果**:
- 请求被正确发送到 http://localhost:8000
- 响应状态码 200/201
- 前端成功接收和显示数据

#### 步骤 8.2: WebSocket 连接（如果实现）
- [ ] 查看前端是否有实时通知或更新
- [ ] 在 Network 标签中查找 WS 连接
- [ ] 执行某个操作并观察是否有实时更新

**期望结果**:
- 建立 WebSocket 连接
- 接收实时消息
- 前端实时更新数据

---

## 📊 测试结果记录

### 功能测试结果

| 功能 | 测试项 | 结果 | 备注 |
|------|--------|------|------|
| **前端应用** | 加载和显示 | ✅/❌ | |
| | 导航菜单 | ✅/❌ | |
| | 响应式设计 | ✅/❌ | |
| **后端 API** | 文档访问 | ✅/❌ | |
| | 健康检查 | ✅/❌ | |
| | 工作流 CRUD | ✅/❌ | |
| **用户认证** | 注册 | ✅/❌ | |
| | 登录/Token | ✅/❌ | |
| | 授权 | ✅/❌ | |
| **模板和市场** | 列表获取 | ✅/❌ | |
| | 特色模板 | ✅/❌ | |
| | 详情查看 | ✅/❌ | |
| **缓存性能** | 首次查询 | ___ ms | |
| | 缓存查询 | ___ ms | |
| | 加速倍数 | ___ x | 目标: >10x |
| **错误处理** | 404 错误 | ✅/❌ | |
| | 401 错误 | ✅/❌ | |
| | 验证错误 | ✅/❌ | |
| **前后端集成** | API 调用 | ✅/❌ | |
| | WebSocket | ✅/❌ | |

### 性能测试结果

| 指标 | 目标 | 实际 | 状态 |
|------|------|------|------|
| 首屏加载 | < 3s | ___ | ✅/❌ |
| API 响应 | < 100ms | ___ | ✅/❌ |
| 缓存命中 | 20-50x | ___ x | ✅/❌ |
| 缓存率 | > 80% | ___ % | ✅/❌ |

---

## 🐛 问题记录

### 问题 1
- **描述**: 
- **重现步骤**: 
- **预期行为**: 
- **实际行为**: 
- **严重程度**: 🔴 严重 / 🟡 中等 / 🟢 轻微

### 问题 2
- **描述**: 
- **重现步骤**: 
- **预期行为**: 
- **实际行为**: 
- **严重程度**: 🔴 严重 / 🟡 中等 / 🟢 轻微

---

## 💡 观察和反馈

### 正面反馈
- 
- 
- 

### 改进建议
- 
- 
- 

### 其他注意事项
- 
- 
- 

---

## 🔗 有用的链接

- **前端应用**: http://localhost:3000
- **后端 API**: http://localhost:8000
- **API 文档**: http://localhost:8000/docs
- **API ReDoc**: http://localhost:8000/redoc
- **MinIO UI**: http://localhost:9000

---

## ✅ 测试完成清单

- [ ] 前端基本功能测试完成
- [ ] 后端 API 文档验证完成
- [ ] 用户认证功能测试完成
- [ ] 工作流 CRUD 操作测试完成
- [ ] 模板和市场功能测试完成
- [ ] 缓存性能测试完成
- [ ] 错误处理测试完成
- [ ] 前后端集成测试完成
- [ ] 所有结果已记录
- [ ] 问题已报告

---

## 📝 测试总结

**测试日期**: 2026-01-03  
**测试者**: _____________  
**总体结论**: 

```
总测试项: ___
通过: ___
失败: ___
通过率: ____%
```

**建议**:
- 
- 
- 

---

**测试指南版本**: v1.0  
**项目**: TenMuses v2.4.0  
**最后更新**: 2026-01-03

